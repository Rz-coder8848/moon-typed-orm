name = "Rz-coder8848/moon-typed-orm"

version = "0.1.0"

readme = "README.mbt.md"

repository = "https://github.com/Rz-coder8848/moon-typed-orm"

license = "Apache-2.0"

keywords = [ "orm", "sql", "query-builder", "database" ]

preferred_target = "wasm"

description = "A typed, injection-safe SQL query builder with a pluggable connection boundary and a dependency-free in-memory backend."
