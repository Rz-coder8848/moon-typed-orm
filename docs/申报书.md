# Moon-Typed-ORM 项目申报书

**基本信息**

- 项目名称：Moon-Typed-ORM —— 编译期类型安全、参数化的 SQL 查询构建器
- 参赛者：王仁政
- 联系方式：13068386886@163.com
- GitHub 仓库链接：https://github.com/Rz-coder8848/moon-typed-orm
- 项目方向：MoonBit 数据库访问层 / ORM 查询构建器
- 是否为移植项目：否（原创）
- 开发时长：从 2026-09-10 至今

**项目简介**

MoonBit 生态里已经有 `moonorm`（`moonbitstack/moonorm`，SQLAlchemy 式 ORM，带 Session、模型、迁移）和它底下的 `moondb` 查询层。我这个项目刻意走另一条路：它靠运行时模型 + Session，我靠编译期类型列 + 可替换后端。手写 SQL 有两个地方老出问题：字符串拼接留注入口子，列名或类型写错要到运行时才报。所以我把查询做成带类型的列 DSL——`Column[Int]` 只能跟 `Int` 比较，传 `String` 编译不过；所有值都转成 `?` 占位符加参数数组，没有插值的口子。后端通过 `Connection` trait 替换，内置零依赖的内存后端（wasm-gc 就能跑）和一个 SQLite 驱动（native）。从 `311a610` 起手至今，一共 20 多个 commit。

选 MoonBit 的原因：最初通过朋友介绍，现在我是一个 MoonBit 爱好者，我想写一个 MoonBit 数据库访问层 / ORM 查询构建器。

**核心功能范围**

- `Select` / `Insert` / `Update` / `Delete` 四个构建器，渲染成 `Statement { sql, params }`，值永远走参数绑定。
- 条件 DSL：`eq / ne / gt / gte / lt / lte / like / in_ / is_null / is_not_null`，组合 `and_ / or_ / not_`，逃逸口 `Condition::raw`。
- 聚合 `count / sum / avg / min / max`，`group_by / having`，四种 JOIN（`join / left_join / right_join / full_join`），内存后端四种都真求值，缺侧填 null。
- 子查询：`IN`（`in_select`）、`EXISTS`（`Condition::exists`）、标量（`eq_sub` 到 `lte_sub`）、相关子查询（引用外层行，单层）。
- 两个后端走同一个 `Connection` trait：`Memory`（零依赖）+ `Sqlite`（vendor amalgamation，native）；事务走可选 `Transactional` trait。
- 测试 48 个 wasm-gc 用例 + 3 个 native 用例全绿，TDD 写的，渲染用例钉死 SQL 文本和参数顺序；`cmd/demo` 一条命令跑完整个功能面。

**限制与取舍**

- 相关子查询只支持单层，不追 grand-outer；一次一个 JOIN。
- 没有 Postgres 驱动，没有 migration / schema 管理。
- 代码量约 2100 行有效 MoonBit（另加约 1000 行测试），因为查询构建器本身是个聚焦的小库，这个体量是它该有的样子。

**移植或参考说明**

- 本项目原创，无移植来源。
- 本项目许可证：Apache-2.0（`moon.mod` 已声明，仓库根目录有 `LICENSE` 文件）。
- 唯一引用的第三方代码是 SQLite amalgamation（`sqlite/sqlite3.c` + `sqlite3.h`，用于 native 后端），SQLite 为 public domain，这个说明已经写进 README 的 License 一节。
